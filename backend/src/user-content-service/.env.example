# ── user-content-service environment variables ────────────────────────────────
# Copy to .env and fill in secrets before running locally.
# Docker deployments override these via compose environment: blocks.

# ── Server ports ──────────────────────────────────────────────────────────────
PORT=5002
GRPC_PORT=18083

# ── Database ──────────────────────────────────────────────────────────────────
ConnectionStrings__DefaultConnection=Host=localhost;Port=5434;Database=content_db;Username=postgres;Password=postgres

# ── Kafka ──────────────────────────────────────────────────────────────────────
# Local dev: use EXTERNAL listener (29092). Docker: kafka:9092
KAFKA_BOOTSTRAP_SERVERS=localhost:29092
