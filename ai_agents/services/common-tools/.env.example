# Shared config across all services
BACKEND_BASE_URL=http://cv-api-gateway:8080

# Provider API keys
GROQ_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=
MISTRAL_API_KEY=

# Global LLM overrides (optional — each agent has its own default)
# Set these to switch all agents to a different provider/model at once
# LLM_PROVIDER=openai
# LLM_MODEL=gpt-4o
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
DATABASE_URL=
