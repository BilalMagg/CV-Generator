name: bilal-vm1

networks:
  cv-network:
    name: cv-network

services:
  frontend:
    image: bilalmagg/frontend:latest
    container_name: cv-frontend
    ports:
      - "4200:80"
    environment:
      - GATEWAY_HOST=cv-api-gateway
      - GATEWAY_PORT=8080
      - FRONTEND_HOST=localhost
      - FRONTEND_PORT=4200
    networks:
      - cv-network

  api-gateway:
    image: bilalmagg/api-gateway:latest
    container_name: cv-api-gateway
    ports:
      - "8080:8080"
    environment:
      - KEYCLOAK_HOST=cv-keycloak
      - KEYCLOAK_PORT=8080
      - KEYCLOAK_EXTERNAL_HOST=localhost
      - KEYCLOAK_EXTERNAL_PORT=9090
      - KEYCLOAK_REALM=cv-realm
      - KEYCLOAK_CLIENT_ID=cv-gateway
      - KEYCLOAK_CLIENT_SECRET=change-me-in-production
      - GATEWAY_HOST=cv-api-gateway
      - GATEWAY_PORT=8080
      - USER_SERVICE_HOST=cv-user-service
      - USER_SERVICE_PORT=8082
      - KEYCLOAK_ADMIN_USERNAME=admin
      - KEYCLOAK_ADMIN_PASSWORD=admin
      - KAFKA_HOST=cv-kafka
      - KAFKA_PORT=9092
      - FRONTEND_HOST=localhost
      - FRONTEND_PORT=4200
    networks:
      - cv-network

  node_exporter:
    image: prom/node-exporter:latest
    container_name: node_exporter
    restart: unless-stopped
    network_mode: host
    pid: host
    deploy:
      resources:
        limits:
          memory: 128M
    volumes:
      - /:/rootfs:ro,rslave

  cadvisor:
    image: gcr.io/cadvisor/cadvisor:latest
    container_name: cadvisor
    restart: unless-stopped
    ports:
      - "7010:8080"
    deploy:
      resources:
        limits:
          memory: 256M
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:rw
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
